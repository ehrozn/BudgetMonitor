# 📤 Инструкция по загрузке на GitHub

## Шаг 1: Подготовка (УЖЕ ВЫПОЛНЕНО ✅)

Эта папка содержит **ТОЛЬКО необходимые файлы** для GitHub:
- ✅ Исходный код приложения
- ✅ Конфигурационные файлы
- ✅ GitHub Actions для автоматической сборки
- ❌ БЕЗ `node_modules` (300+ MB)
- ❌ БЕЗ `package-lock.json`

**Размер папки:** ~1 MB (вместо 300+ MB!)

---

## Шаг 2: Создание репозитория на GitHub

1. Перейдите на [github.com](https://github.com)
2. Нажмите **"New repository"** (зеленая кнопка)
3. Укажите название: `budget-manager-app` (или любое другое)
4. Выберите **Private** (приватный репозиторий)
5. **НЕ ДОБАВЛЯЙТЕ** README, .gitignore, license (они уже есть!)
6. Нажмите **"Create repository"**

---

## Шаг 3: Загрузка файлов

### Вариант А: Через командную строку (рекомендуется)

Откройте PowerShell в папке `BudgetApp_GitHub` и выполните:

```powershell
# Инициализация Git
git init

# Добавление всех файлов
git add .

# Первый коммит
git commit -m "Initial commit: Budget Manager App"

# Подключение к вашему репозиторию (ЗАМЕНИТЕ URL!)
git remote add origin https://github.com/ВАШ_ЛОГИН/budget-manager-app.git

# Загрузка на GitHub
git branch -M main
git push -u origin main
```

### Вариант Б: Через GitHub Desktop

1. Скачайте [GitHub Desktop](https://desktop.github.com/)
2. Откройте приложение → **"Add" → "Add Existing Repository"**
3. Выберите папку `BudgetApp_GitHub`
4. Нажмите **"Publish repository"**

### Вариант В: Через веб-интерфейс GitHub

1. Создайте ZIP-архив папки `BudgetApp_GitHub`
2. На странице вашего пустого репозитория нажмите **"uploading an existing file"**
3. Перетащите файлы (НЕ архив, а содержимое папки!)
4. Нажмите **"Commit changes"**

---

## Шаг 4: Настройка автоматической сборки

### 4.1. Создание Expo аккаунта

1. Перейдите на [expo.dev](https://expo.dev)
2. Нажмите **"Sign Up"** (регистрация бесплатная!)
3. Подтвердите email

### 4.2. Получение токена

Откройте терминал и выполните:

```bash
# Установка EAS CLI
npm install -g eas-cli

# Вход в аккаунт
npx eas-cli login

# Получение токена
npx eas-cli whoami
```

Скопируйте ваш токен доступа.

### 4.3. Добавление секрета в GitHub

1. Откройте ваш репозиторий на GitHub
2. Перейдите в **Settings** → **Secrets and variables** → **Actions**
3. Нажмите **"New repository secret"**
4. Name: `EXPO_TOKEN`
5. Value: вставьте скопированный токен
6. Нажмите **"Add secret"**

---

## Шаг 5: Запуск сборки

### Автоматическая сборка

После настройки токена, каждый `git push` будет автоматически запускать сборку APK!

```bash
git add .
git commit -m "Update app"
git push
```

### Проверка статуса сборки

1. Откройте репозиторий на GitHub
2. Перейдите во вкладку **"Actions"**
3. Найдите запущенную сборку **"Build Android APK"**
4. Дождитесь завершения (~10-15 минут)

### Скачивание APK

1. После завершения сборки откройте её
2. Прокрутите вниз до раздела **"Artifacts"**
3. Скачайте **"android-apk"**
4. Распакуйте ZIP → получите готовый APK файл!

---

## 🎉 Готово!

Теперь вы можете:
- ✅ Установить APK на телефон Android
- ✅ Автоматически собирать новые версии через GitHub
- ✅ Делиться APK с другими пользователями

---

## ❓ Частые проблемы

### "Repository is too large"
**Решение:** Убедитесь, что загружаете папку `BudgetApp_GitHub`, а НЕ `android_app` (она без node_modules)

### "Workflow failed"
**Решение:** Проверьте, добавили ли вы секрет `EXPO_TOKEN` в настройках репозитория

### "Authentication failed"
**Решение:** 
1. Создайте Personal Access Token на GitHub
2. Settings → Developer settings → Personal access tokens → Generate new token
3. Используйте токен вместо пароля при `git push`

---

## 🆘 Нужна помощь?

Если что-то не получается, проверьте:
1. `.gitignore` файл присутствует (он исключает node_modules)
2. Файл `.github/workflows/build-android.yml` загружен
3. Секрет `EXPO_TOKEN` правильно добавлен
4. У вас есть активный аккаунт на expo.dev
