# 💰 Budget Manager - Приложение для управления бюджетом

Мобильное приложение для Android, созданное с помощью React Native и Expo.

## 📱 Основные возможности

- ✅ Управление счетами и подсчетами (карманами)
- ✅ Отслеживание доходов и расходов
- ✅ Категории транзакций с иконками
- ✅ Автоматическое распределение доходов
- ✅ Умные переводы между счетами
- ✅ Повторяющиеся транзакции
- ✅ Графики и аналитика расходов
- ✅ Резервное копирование и восстановление данных

## 🚀 Установка зависимостей

После клонирования репозитория выполните:

```bash
npm install
```

## 🔧 Запуск в режиме разработки

### Expo Go (для быстрого тестирования)

```bash
npm start
```

Затем отсканируйте QR-код в приложении Expo Go на вашем телефоне.

## 📦 Сборка APK

### Через GitHub Actions (автоматически)

1. Push в ветку `main` или `master`
2. GitHub Actions автоматически соберет APK
3. Скачайте готовый APK из вкладки "Actions" → "Artifacts"

### Локально через EAS Build

```bash
npm install -g eas-cli
eas login
eas build --platform android --profile preview
```

**Примечание:** Для EAS Build требуется аккаунт на [expo.dev](https://expo.dev) (бесплатно, 30 сборок/месяц)

## ⚙️ Конфигурация

- `app.json` - основные настройки приложения
- `eas.json` - профили сборки для EAS
- `.github/workflows/build-android.yml` - автоматическая сборка через GitHub Actions

## 📝 Технологии

- **React Native** - кросс-платформенная разработка
- **Expo** - инструменты и SDK
- **Expo Router** - навигация на основе файловой системы
- **AsyncStorage** - локальное хранилище данных
- **Lucide Icons** - красивые иконки
- **React Native Reanimated** - плавные анимации

## 📂 Структура проекта

```
app/          # Страницы приложения (файловая маршрутизация)
components/   # Переиспользуемые компоненты
constants/    # Константы (категории, цвета, иконки)
context/      # React Context для управления состоянием
utils/        # Вспомогательные функции
assets/       # Изображения, шрифты
```

## 🔐 Требования для GitHub Actions

Для автоматической сборки через GitHub Actions необходимо:

1. Создать аккаунт на [expo.dev](https://expo.dev)
2. Получить токен: `npx eas-cli login` → `npx eas-cli whoami`
3. Добавить секрет `EXPO_TOKEN` в Settings → Secrets → Actions вашего репозитория

## 📄 Лицензия

Частный проект
